import React from 'react';
import { Link } from 'react-router-dom';

const Logo = () => {
    return (
        <div>
            <Link to='/'>
                SO
            </Link>
        </div>
    );
};

export default Logo;